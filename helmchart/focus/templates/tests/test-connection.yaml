apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "focus.fullname" . }}-test-connection"
  labels:
    {{- include "focus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "focus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
